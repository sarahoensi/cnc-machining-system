// features/right_triangle/solveTriangle.ts

import { TriangleKey } from "./triangleForm";

export function solveTriangle(
  input: Record<TriangleKey, number>
): Partial<Record<TriangleKey, number>> {

  // 🔵 kall Rust her
  // const result = await rustApi.solveTriangle(input)

  return {
    c: 5, // eksempel
  };
}